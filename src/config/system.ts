/**
 * @desc 导出window
 * */
export const win: Window = window || (window as any)

/**
 * @desc 导出document
 * */
export const doc: Document = document || (document as any)

/**
 * @desc 导出Navigator
 * */
export const nav: Navigator = navigator || (navigator as any)
